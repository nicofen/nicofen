(require 2htdp/image)

;; function-definitions-starter.rkt

;(define (bulb c)
  ;(circle 40 "solid" c))

;(bulb (string-append "re" "d"))
;(bulb "red")
;(circle 40 "solid" "red")




(define (foo a b)
  (+ (* 3 a)
     b
    (* b a)))
